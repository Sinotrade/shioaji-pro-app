import { globalStyle, style } from '@vanilla-extract/css';
import { vars } from '../theme.css';
export const wrap = style({ display: 'flex', flexDirection: 'column', gap: 12, minWidth: 0, color: vars.color.foreground, fontSize: '0.75rem', lineHeight: 1.6 });
export const header = style({ display: 'flex', flexWrap: 'wrap', alignItems: 'center', justifyContent: 'space-between', gap: 8 });
export const title = style({ margin: 0, fontSize: '1rem', fontWeight: 650 });
export const subtitle = style({ margin: '4px 0 0', fontSize: '0.8rem' });
export const caption = style({ margin: 0, fontSize: '0.7rem', color: vars.color.mutedForeground, overflowWrap: 'anywhere' });
export const label = style({ fontSize: '0.72rem', color: vars.color.mutedForeground });
export const cards = style({ display: 'grid', gridTemplateColumns: 'repeat(auto-fit, minmax(min(100%, 210px), 1fr))', gap: 10 });
export const card = style({ display: 'flex', flexDirection: 'column', gap: 5, padding: 12, minWidth: 0, background: vars.color.inset, border: `1px solid ${vars.color.border}`, borderRadius: 8, selectors: { '&[data-tone="warning"]': { borderColor: vars.color.amber }, '&[data-tone="danger"]': { borderColor: vars.color.danger } } });
export const number = style({ fontFamily: vars.font.mono, fontVariantNumeric: 'tabular-nums', fontSize: '1.6rem', lineHeight: 1.3 });
export const track = style({ height: 5, borderRadius: 4, background: vars.color.border, overflow: 'hidden' });
globalStyle(`${track} > div`, { height: '100%', background: vars.color.accent });
globalStyle(`${card}[data-tone="warning"] ${track} > div`, { background: vars.color.amber });
globalStyle(`${card}[data-tone="danger"] ${track} > div`, { background: vars.color.danger });
globalStyle(`${number} small`, { fontSize: '0.7rem', fontWeight: 400 });
export const status = style({ display: 'flex', flexWrap: 'wrap', justifyContent: 'space-between', gap: 4, fontSize: '0.65rem', color: vars.color.mutedForeground });
export const note = style({ margin: 0, fontSize: '0.7rem', color: vars.color.mutedForeground, borderLeft: `2px solid ${vars.color.borderBright}`, paddingLeft: 10 });
export const warning = style({ color: vars.color.amber, fontSize: '0.7rem', border: `1px solid ${vars.color.amber}`, borderRadius: 5, padding: '8px 10px', overflowWrap: 'anywhere' });
export const button = style({ background: vars.color.inset, color: vars.color.foreground, border: `1px solid ${vars.color.border}`, borderRadius: 5, padding: '6px 10px', cursor: 'pointer', textAlign: 'left', font: 'inherit' });
export const summary = style({ display: 'flex', flexWrap: 'wrap', gap: '6px 20px', fontSize: '0.72rem' });
export const details = style({ borderTop: `1px solid ${vars.color.border}`, paddingTop: 10 });
globalStyle(`${details} > summary`, { cursor: 'pointer', fontWeight: 600, marginBottom: 6 });
export const endpoint = style({ display: 'flex', flexDirection: 'column', padding: '7px 0', borderBottom: `1px solid ${vars.color.border}`, fontSize: '0.65rem', overflowWrap: 'anywhere' });
export const frame = style({ width: '100%', minHeight: 680, flexShrink: 0, border: `1px solid ${vars.color.border}`, borderRadius: 6 });
