# Optional: override REST/SSE base URL for remote rshioaji servers.
# Leave unset when using the dev proxy or when deployed under /apps/<name>/.
# VITE_API_BASE=http://my-rshioaji.local:8080

# Optional: build-time public base path when deploying under a subpath.
# VITE_BASE=/apps/my-app/
