# CONTEXT.md — Ubiquitous Language

Shioaji Pro 的詞彙表。只放詞彙定義,不放實作細節。

## 指數成分 (Index Components)

- **建底查詢 (bootstrap query)**: 對 `index_components` 的一次性權威查詢,唯二時機:該指數尚無本地狀態時、日切時。絕不用於定時輪詢、重連恢復或畫面刷新。
- **日切 (day rollover)**: 從串流事件攜帶的交易日欄位偵測到新交易日(資料驅動,不依賴牆上時鐘)。與現貨當日走勢的 session 判定同一哲學。
- **投影 (projection)**: 一條 `(指數, 投影)` 獨立訂閱主題;事件為該投影的整包替換,無增量、無回放。
- **產業全景 (industry panorama)**: 由類股熱力圖面板升級而成的產業地圖面板 — 全景層(treemap)+下鑽層(單一產業詳情)。與市場脈動的分工:市場脈動=誰在動指數(貢獻敘事),產業全景=錢在哪裡、誰強誰弱(地圖)。
- **主力貢獻 (main contributors)**: 單一產業內依 |貢獻點| 排序的前 10 檔成分股(該群組的 AbsDesc10 投影)。
- **餘量 (remainder)**: 群組總值減去已列示成員合計的差額,以「其他」列/磚呈現;一律由已訂閱的群組總值推導,不另外訂閱。

## 策略研究 (Strategy Research)

- **策略資產 (strategy asset)**: 策略可讀取資料、產生部位意圖及歸因結果的具名商品。
- **商品集合 (universe)**: 一次策略執行可使用的策略資產集合;單商品策略是只含一個商品的集合,不是另一種引擎模式。
- **主商品 (primary asset)**: 商品集合中提供簡寫資料欄位與向後相容訊號語法的預設策略資產。
- **批次執行 (batch run)**: 將同一策略分別套用到多個互不共享資金與持倉的商品,再彙整獨立結果;不是投資組合策略。
- **投資組合執行 (portfolio run)**: 策略在同一時間軸與共享資金限制下共同讀取、配置及歸因多個策略資產。
- **策略成品 (strategy artifact)**: 使用者持續辨識與管理的一個策略,可包含多個不可變策略修訂。
- **策略修訂 (strategy revision)**: 策略程式、參數結構、資料需求與執行語意的不可變版本。
- **策略意圖 (strategy intent)**: 策略在決策週期輸出的部位變更要求;尚未經過訂單規劃、風控與成交模擬,不是券商委託。
- **策略執行 (strategy run)**: 以固定策略修訂、已解析商品集合、資料身分與執行假設產生的可重現研究紀錄。
- **前向測試 (forward test)**: 使用即時行情計算訊號及模擬成交的策略執行;不送出真實券商委託。
