# CONTEXT.md — Ubiquitous Language

Shioaji Pro 的詞彙表。只放詞彙定義,不放實作細節。

## 指數成分 (Index Components)

- **建底查詢 (bootstrap query)**: 對 `index_components` 的一次性權威查詢,唯二時機:該指數尚無本地狀態時、日切時。絕不用於定時輪詢、重連恢復或畫面刷新。
- **日切 (day rollover)**: 從串流事件攜帶的交易日欄位偵測到新交易日(資料驅動,不依賴牆上時鐘)。與現貨當日走勢的 session 判定同一哲學。
- **投影 (projection)**: 一條 `(指數, 投影)` 獨立訂閱主題;事件為該投影的整包替換,無增量、無回放。
- **產業全景 (industry panorama)**: 由類股熱力圖面板升級而成的產業地圖面板 — 全景層(treemap)+下鑽層(單一產業詳情)。與市場脈動的分工:市場脈動=誰在動指數(貢獻敘事),產業全景=錢在哪裡、誰強誰弱(地圖)。
- **主力貢獻 (main contributors)**: 單一產業內依 |貢獻點| 排序的前 10 檔成分股(該群組的 AbsDesc10 投影)。
- **餘量 (remainder)**: 群組總值減去已列示成員合計的差額,以「其他」列/磚呈現;一律由已訂閱的群組總值推導,不另外訂閱。

## 圖表畫圖 (Chart Drawings)

- **畫圖物件 (drawing)**: 使用者畫在 K 線主圖上的自由物件(水平線、趨勢線、射線、延伸線、方框)。只是標註,不參與任何下單或觸價邏輯。
- **錨點 (anchor)**: 畫圖物件的控制點,一律以 `(時間, 價格)` 保存,絕不存 K 棒 index — 這是切換週期後位置不變的前提。投影時把時間內插成小數 logical index 再換座標,因為 `timeToCoordinate()` 只認 K 棒格點上的時間。
- **商品鍵 (symbol key)**: 畫圖物件的保存單位。期貨預設收斂到根代碼(TXFR1、TXFI6、TXFJ6 → TXF),連續月只是近月別名,換月後畫的壓力線仍然有效;可在樣式面板關閉改為每個合約代碼各自獨立。選擇權不收斂 — 不同履約價是不同商品。

## 價格線標籤 (Chart Line Badges)

- **價格線標籤 (badge)**: 畫在主圖右緣、貼著價格軸的互動色塊,一個標籤對應一條價格線(委託單、停損、停利、警示、持倉均價)。內容是「文字 ＋ 拖曳握把 ＋ ✕」,顏色與線同色。版面與命中判定在 `lib/chart-line-badges`(純函式),繪製在 `lib/chart-line-badge-layer`(series primitive)。
- **上膛 (arming)**: 持倉標籤的 ✕ 第一下只切到待確認狀態、第二下才送出市價平倉,5 秒未按第二下自動退膛。市價平倉不可逆,與持倉 tab 的 arm-lock 是同一套防誤觸思路。
- **保護單 (protective trigger)**: 停損與停利觸價單。平倉送出後必須連帶撤掉同商品的保護單,否則部位已平、觸價時會開出一筆反向新倉。警示不算保護單(只通知,不送單),平倉後保留。
