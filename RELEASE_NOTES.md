## v0.1.37 - Shioaji Server 1.7.2、單一行情連線與一鍵本機 HTTPS

### Shioaji Server 升級 v1.7.2
- 內建 server 升級至 `v1.7.2`，支援 graceful shutdown 與 HTTPS＋HTTP/2。
- 盤中實測：指數自算、成分股貢獻、產業貢獻與市場訊號全數正常。

### 單一行情連線（SSE 整併）
- 所有即時資料（行情、指數加值、市場訊號、委託回報、合約事件）整併至單一 aggregate 串流連線，原本最多 6 條連線降為 1 條。
- 大幅釋放每人 5 條的 API 連線額度，瀏覽器同時開多分頁也不再撞上 HTTP/1.1 同源六連線上限。
- 開盤時段伺服器剛啟動的訂閱競態已處理：訂閱請求逾時自動退避重試，市場脈動不再卡在「訂閱中」。

### 借券賣出（SBL）
- 下單面板信用選擇器新增「借券賣出」與「借券賣出（豁免平盤下限制）」，委託類別 5／6 完整落入回報。

### 本機 HTTPS 一鍵啟用
- 伺服器管理新增「啟用本機 HTTPS」：內建 mkcert，一鍵產生並信任 localhost／127.0.0.1 憑證，**不需自行安裝任何工具**。
- 首次啟用會跳出一次系統的憑證信任視窗（macOS 輸入登入密碼、Windows 按確認），之後即可用 `https://localhost:<port>` 以 HTTPS＋HTTP/2 連本機伺服器。
- 停用一鍵改回 HTTP，切換皆一次收斂。

### 穩定性
- 伺服器輸出改由原生層寫入日誌檔（`~/.shioaji/sjpro-server-<port>.log`），修正長時間使用後狀態誤判「未運行」與健康伺服器被反覆重啟的問題。
- 伺服器啟動、模擬／正式切換、HTTPS 切換等過渡期的重啟循環全數修正；App 啟動時會直接接管既有的健康伺服器，不再誤殺重啟。
- 伺服器狀態列排版修正（標題與版本字不再斷行）。

### 相容性
- 內建 Shioaji Server `v1.7.2`。
- 深色、純黑與淺色主題完整支援。

---

⚠ 回測結果基於歷史資料與簡化成本假設，不代表未來績效；AI 分析僅供參考；自動下單請自行評估風險，盈虧自負。

Shioaji Pro 桌面版 - 內建 shioaji server（sidecar）、伺服器管理介面、系統匣、自動更新。

下載：macOS `.dmg` | Windows `.msi` / `.exe` | Linux `.AppImage` / `.deb` / `.rpm`
