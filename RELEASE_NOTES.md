## v0.1.34 — Shioaji 1.7.1 即時市場脈動

### 市場脈動
- 新增「市場脈動」面板，支援加權與櫃買自算指數、成分股貢獻排行及產業貢獻。
- 支援開盤前試撮資料並明確標示「試撮」，避免與正式成交行情混淆。
- 成分股排行可切換前十、影響最大、拉抬與壓抑，點選代碼會連動既有 K 線與下單面板。
- 新增 TSE／OTC 全市場即時訊號，可切換漲跌停、急漲急跌與單筆爆量，並保留重連遺漏警示。

### 串流穩定性
- 新增專屬串流訂閱生命週期；Server 重啟、SSE 重連及每日維護後會自動恢復指數貢獻與市場訊號訂閱。
- 市場訊號會去重並限制記憶體保留筆數，scanner gap 事件會明確提示遺漏筆數。

### 內建 Server
- 內建 Shioaji Server 升級至 `v1.7.1`，並以實際 OpenAPI 驗證新訂閱格式。

---

⚠ 回測結果基於歷史資料與簡化成本假設，不代表未來績效；AI 分析僅供參考；自動下單請自行評估風險，盈虧自負。

Shioaji Pro 桌面版 — 內建 shioaji server（sidecar）、伺服器管理介面、系統匣、自動更新。

下載：macOS `.dmg` ｜ Windows `.msi` / `.exe` ｜ Linux `.AppImage` / `.deb` / `.rpm`
