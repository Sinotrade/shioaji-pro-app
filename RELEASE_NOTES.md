## v0.1.36 - 市場脈動貢獻視圖重做與面板庫

![市場脈動影響最大與貢獻傳導](https://raw.githubusercontent.com/Sinotrade/shioaji-pro-app/v0.1.36/docs/images/release-0.1.36-market-pulse.png)

### 市場脈動
- 成分股貢獻檢視重做：排行控制移入區塊標題列，簡化為「影響最大」（絕對值前十，預設）與「多空對照」（上漲／下跌 Top 25 左右並排）。
- 兩種檢視皆有貢獻點數比例長條：影響最大以「貢獻（點）／漲跌幅」欄間分隔為零軸（跌向左、漲向右），多空對照維持自中軸向兩側發散。
- 訂閱固定為 positive25＋negative25 兩條，影響最大與前十皆由前端推導 —— 切換檢視零延遲，不再重新訂閱閃爍。
- 官方指數與台指近月補上漲跌點數與百分比，與自算指數同樣套用方向色；時間差改為中性色（資料領先度指標，非多空方向）。
- 貢獻傳導文字改用 rem 並隨字級設定等比縮放（含版面幾何），調大字級後傳導圖不再是小字；「漲跌幅」開關移入貢獻傳導標題列。
- 多空對照補齊「貢獻（點）／漲跌幅」欄位標題；爆量金額單位改用萬／億。

### 面板庫（新增面板重做）

![面板庫與即時預覽](https://raw.githubusercontent.com/Sinotrade/shioaji-pro-app/v0.1.36/docs/images/release-0.1.36-panel-library.png)

- 「＋新增面板」由下拉選單改為可搜尋的面板庫：名稱與用途關鍵字搜尋、最近使用列、五大分類卡片（每個面板附一行用途描述），面板數量再多也不會超出畫面。
- 鍵盤操作：↑↓ 移動、Enter 新增、Shift+Enter 連續新增、Esc 關閉。
- 已存在的單例面板改為「點擊前往」：自動捲動至該面板並高亮提示。
- 滑鼠懸停或鍵盤選取顯示示意圖預覽；「即時預覽」可載入縮放後的真實面板（一次一個、唯讀，瀏覽清單不會佔用行情連線）。
- Cmd+K 指令面板同步支援「新增面板：××」命令，排在商品搜尋結果之後。

### 相容性
- 內建 Shioaji Server 維持 `v1.7.1`。
- 深色、純黑與淺色主題完整支援新檢視與面板庫。

---

⚠ 回測結果基於歷史資料與簡化成本假設，不代表未來績效；AI 分析僅供參考；自動下單請自行評估風險，盈虧自負。

Shioaji Pro 桌面版 - 內建 shioaji server（sidecar）、伺服器管理介面、系統匣、自動更新。

下載：macOS `.dmg` | Windows `.msi` / `.exe` | Linux `.AppImage` / `.deb` / `.rpm`
