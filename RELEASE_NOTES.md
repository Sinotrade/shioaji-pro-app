## v0.1.42 - 組合單全面重做、組合商品面板與超寬螢幕支援

### 組合單全面重做（#32）

回報者實測發現舊組合單「整體買賣別不起作用」— 舊模式下伺服器只認兩腳各自的買賣別，面板上的買進/賣出組合鈕形同虛設，買賣相反的誤操作風險是真金白銀。本版全面改為 Shioaji v1.7.3 的 managed 組合語意：

- **腳不再各帶買賣別**：整體「買進組合／賣出組合」是唯一方向，兩腳實際買賣依期交所組合規則展開（買進跨月價差＝賣近月＋買遠月），**每腳的實際方向在下單前全程可見** — 腳標籤、方向摘要、確認鈕都會顯示。
- **期貨組合顯示組合商品檔原生報價**：不再用兩腳合成 — 兩腳輸入後自動經伺服器驗證取得真實組合合約（如 `TXFI6/J6`），顯示原生組合簿五檔梯（含量能條、Σ買賣、價差），**點檔位價直接帶入淨價**。合成價降為參考、僅在無原生簿時顯示。
- **反序自動換序**：兩腳順序打反會自動換成期交所 canonical 順序；同履約價 Call＋Put（跨式 vs 轉換/逆轉）強制明選型別，未選擋下單。
- 型別、近/遠月份、組合代碼以標記清楚呈現；淨價可為 0 或負值，空白淨價不再默默變成 0 送出。

![組合商品與組合單](https://raw.githubusercontent.com/Sinotrade/shioaji-pro-app/v0.1.42/docs/images/release-0.1.42-combo.png)

### 組合商品面板（新）

自選清單支援組合商品前的主要動線 — 交易所目前所有可交易的組合一目了然：

- 依家族枚舉全部可交易跨月價差（`GET 組合商品檔`），**月份直接顯示「9月/10月」**、代碼降為次行，不用背月份碼。
- 委買/委賣/成交/總量每 5 秒批次更新；有市場的排前面、單邊空簿明確顯示。
- **點一列 → 兩腳直接帶入組合單**，同時 K 線、五檔、分時等連動面板跟著切換（見下）。
- 家族選擇：常用指數置頂的下拉＋**個股期搜尋器**（打代碼或名稱直接切到該股的個股期家族）＋標準連動/釘選（其他面板點到期貨或有個股期的股票時自動跟隨；個股期選擇器面板同步接上同一套連動）。

### 選擇權策略快建（新）

組合單上的「策略快建」— 專業選擇權建倉動線，全程不用讀代碼：

- 商品（臺指選、週選、個股選全支援）→ 月份 → 策略（**價差／跨式／勒式／轉逆／跨月**）→ 履約價（預設貼 ATM）。
- 選完兩腳自動帶入 canonical 順序；跨式/轉逆的型別意圖直接落地，不用再手選。

![選擇權策略快建](https://raw.githubusercontent.com/Sinotrade/shioaji-pro-app/v0.1.42/docs/images/release-0.1.42-strategy.png)

### 組合商品連動 K 線／五檔／分時

組合商品現在是一級行情商品：點組合商品面板任一列，K 線面板直接載入**價差本身的歷史 K 棒與即時走勢**、五檔面板顯示組合簿、分時同理 — 走 app 標準連動/釘選機制。一般下單／網格／閃電面板碰到組合會導引改用組合單；K 線圖上的點價交易對組合停用（保留警示）。

![組合 K 線與五檔連動](https://raw.githubusercontent.com/Sinotrade/shioaji-pro-app/v0.1.42/docs/images/release-0.1.42-combo-linked.png)

### 超寬螢幕版面密度

- 版面欄寬與面板最小寬**不再隨螢幕等比放大**：超寬螢幕（~1920 起、含 3440 帶魚屏）自動採用更細的欄距，自選清單等窄面板終於可以縮到合理寬度，拖拉步進也更精細。
- 筆電（<1800px）行為完全不變；既有版面自動無損升級，跨螢幕開啟不會變形或跳列。

### 頂部工具列對齊硬化

- 公司網路封鎖 Google Fonts 時（字型回退），頂列不再垂直歪斜 — 對齊改為不依賴字型度量。

### 相容性

- 內建 Shioaji Server `v1.7.3`（組合商品檔下單與報價）。
- 深色、純黑與淺色主題完整支援。

---

⚠ 組合單為真實下單（模擬環境不支援組合單），送出前請確認每腳方向；到價監控會自動送單，請盯緊成交回報。回測結果基於歷史資料與簡化成本假設，不代表未來績效；AI 分析僅供參考；自動下單請自行評估風險，盈虧自負。

Shioaji Pro 桌面版 - 內建 shioaji server（sidecar）、伺服器管理介面、系統匣、自動更新。

下載：macOS `.dmg` | Windows `.msi` / `.exe` | Linux `.AppImage` / `.deb` / `.rpm`
