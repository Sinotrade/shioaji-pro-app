## v0.1.38 - 持倉/委託/帳務全面重做、多帳號與分倉下單、版面庫

![持倉/委託/帳務重設計](https://raw.githubusercontent.com/Sinotrade/shioaji-pro-app/v0.1.38/docs/images/release-0.1.38-dock.png)

### 持倉/委託/帳務面板全面重做
- 全域控制列：帳戶範圍選單（全部帳戶/個別）、合併｜分帳戶雙模式（分帳戶＝可折疊帳號區段＋小計）、市場篩選（全部/證券/期貨）、跨頁籤常駐摘要列（總損益/總市值/期貨權益/風險指標）。
- 持倉：欄位隨面板寬度自適應（窄=兩行卡片、中=精簡表、寬=完整表含昨餘與損益分布）；批次市價平倉（勾選＋筆數確認，全程受防誤觸鎖保護）。
- 委託：成交進度圈、委託/成交均價雙行、狀態篩選（有效/已成交/其他）、批次刪單；保留改價/減量。
- 帳務頁籤重做為「帳務/交割」交易工具：資金狀態＋風險指標色條、交割行事曆 T+0/T+1/T+2、今日已實現損益（可展開明細）、交易額度、預收券款/圈存查詢（模擬環境顯示不支援）。

### 多帳號與分倉下單
- 下單面板帳號 chip：顯示目前路由帳號，多帳號一鍵切換（與全域同步）；正式環境紅框警示。
- 分倉下單：同類型帳戶 ≥2 時可展開——比例或固定數量分配、整數化拆分、送出前預覽每戶數量、分配可存 preset；逐戶送出、逐戶回報（部分失敗不自動反向，由使用者決定後續）。
- 重新登入不再殘留舊帳號快照（#16）：憑證變更即重啟伺服器重新登入、登出即停伺服器、帳號清單可重新整理、未簽署帳戶顯示為灰字提示而非隱藏。

![版面庫](https://raw.githubusercontent.com/Sinotrade/shioaji-pro-app/v0.1.38/docs/images/release-0.1.38-layout-library.png)

### 版面庫與頂欄自訂
- 「版面」重做為版面庫：每個預設版面與自存版面自動生成 layout 縮圖（分類色塊），滑過放大預覽＋面板清單；儲存目前版面時即時預覽、可選 icon 標記。
- 頂欄項目可自訂：加權/基差/銀行水位/連線狀態/時鐘/全開/版面逐項開關（設定 → 外觀）。

### 統一設定與自選清單
- 主題/帳號/版面收斂為單一「設定」分類面板（外觀/音效與隱私/帳號/風控/版面）；風控簡化為頂欄一鍵 Kill Switch，鎖定時紅色警示。
- 自選清單新增「調整順序」模式：拖曳把手＋上下移按鈕，平時停用拖曳避免誤觸；並修正桌面版拖曳事件被攔截導致無法拖動的問題。

### 其他
- 伺服器管理重設計：精簡狀態卡（狀態徽章/啟停/HTTPS 開關）＋完整設定對話框；伺服器狀態誤判與長時間使用後被誤重啟的問題修正。
- 全新 Orb 活動指示器取代所有載入動畫（五種家族、純 CSS）。
- 複製診斷資訊補齊主機架構、伺服器版本、CA 效期等關鍵欄位。

### 相容性
- 內建 Shioaji Server `v1.7.2`。
- 深色、純黑與淺色主題完整支援。

---

⚠ 回測結果基於歷史資料與簡化成本假設，不代表未來績效；AI 分析僅供參考；自動下單請自行評估風險，盈虧自負。

Shioaji Pro 桌面版 - 內建 shioaji server（sidecar）、伺服器管理介面、系統匣、自動更新。

下載：macOS `.dmg` | Windows `.msi` / `.exe` | Linux `.AppImage` / `.deb` / `.rpm`
