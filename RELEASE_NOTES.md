## v0.1.41 - 委託清單修正、視窗適應與安裝範圍選擇

### 委託 tab 修正（#19）
- **委託查詢改按帳戶逐一查詢**：分倉下單或多帳戶環境下，非目前選中帳戶的委託過去不會出現在委託 tab — 現在與持倉一致，所有已簽署帳戶的委託都會列出。
- **計數器與清單一致**：tab 上的「委託 [有效/總數]」改用與清單相同的市場/帳戶範圍統計，不再出現「計數有值、清單卻顯示無委託」的矛盾。
- **篩選狀態透明化**：狀態（有效/成交/失效）與市場（證券/期貨）篩選會記憶上次選擇 — 過去點過一次後清單長期空白且無提示，容易誤判為「委託消失」。現在清單空白時會明確顯示「N 筆委託被篩選隱藏」並提供「顯示全部」一鍵重設。
- **查詢失敗不再閃空**：委託查詢全數失敗（例如伺服器忙碌）時保留上一輪清單，不再瞬間刷成「無委託」。
- **Esc 連按全部刪單同步修正**：全刪單改為對所有已簽署帳戶的掛單送出刪單 — 委託清單看得到的，全刪單就刪得到（此快捷鍵預設關閉，於設定 → 風控開啟）。

### 頂部工具列窄視窗修正（#24）
- 視窗縮到約 1370px 以下時 header 不再折成兩列：文字全面不換行，資訊項（基差、時鐘、銀行水位、加權）依優先序自動收起，互動按鈕（伺服器、風控、新增面板、版面、全開、設定）任何寬度下都保持可見可點。感謝 @how0531 的完整量測與修復（#27）。

### Windows 安裝範圍選擇（#23）
- 安裝程式（`.exe`）新增「僅為我安裝／為所有使用者安裝」選擇頁。公司電腦由 IT 以另一組管理員帳密提權安裝時，請選「所有使用者」— 過去會整包裝進管理員帳號的 profile，一般使用者看不到也無法移除。感謝 @how0531 的根因分析（#26）。
- 已中招的使用者：自動更新不會重建捷徑，請下載本版安裝檔手動重跑一次互動式安裝（並請 IT 清除管理員 profile 下的殘留）。

### 伺服器連線狀態誠實化（#28）
- 行情後端連線（Solace session）中斷自動恢復期間，伺服器按鈕改顯示琥珀色「行情恢復中…」與重試次數（取自 Shioaji Server v1.7.3 新增的健康欄位），不再與實際狀態不符地亮綠燈；恢復完成自動回綠。

### 其他
- 新增回測自訂指標 `ta` 函式庫完整參考文件（`docs/backtest-ta-reference.md`，#30）。

### 相容性
- 內建 Shioaji Server `v1.7.3`。
- 深色、純黑與淺色主題完整支援。

---

⚠ 回測結果基於歷史資料與簡化成本假設，不代表未來績效；AI 分析僅供參考；自動下單請自行評估風險，盈虧自負。

Shioaji Pro 桌面版 - 內建 shioaji server（sidecar）、伺服器管理介面、系統匣、自動更新。

下載：macOS `.dmg` | Windows `.msi` / `.exe` | Linux `.AppImage` / `.deb` / `.rpm`
